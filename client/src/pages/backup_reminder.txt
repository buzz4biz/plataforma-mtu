
                {messages.length > 2 && (
                  <div className="mt-6 p-4 rounded-xl bg-accent/30 border border-primary/20 animate-fade-in">
                    <div className="flex gap-3">
                      <div className="w-5 h-5 rounded-full bg-primary/20 flex items-center justify-center flex-shrink-0 mt-0.5">
                        <span className="text-xs font-semibold text-primary">!</span>
                      </div>
                      <div>
                        <p className="text-sm font-medium text-foreground mb-1">Dica importante:</p>
                        <p className="text-xs text-muted-foreground">
                          Copie e cole as informacoes importantes desta conversa em um documento para ter um backup seguro.
                        </p>
                      </div>
                    </div>
                  </div>
                )}
